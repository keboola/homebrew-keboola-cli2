# homebrew-keboola-cli2

Homebrew tap for the Keboola CLI (`kbagent`).

```sh
brew tap keboola/keboola-cli2 https://github.com/keboola/homebrew-keboola-cli2
brew install keboola-cli2
```

The formula in `Formula/keboola-cli2.rb` is published automatically by the release workflow.
